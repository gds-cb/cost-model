/*!
 * cost-config.js —— 工具箱配置（部署前请填写）
 * ---------------------------------------------------------------
 * 这个文件是「意见反馈」等对外信息的唯一配置入口。
 * 全部留空也能正常运行 —— 反馈按钮会自动降级为「复制内容到剪贴板」。
 *
 * ⚠️ 为什么需要它：本工具是纯前端应用，没有服务器，
 *    因此无法自动收集用户反馈。必须给出一个用户能把内容发过来的渠道。
 */
window.COST_TOOL_CONFIG = {

    /* ============ 反馈渠道（填哪个启用哪个，可以都填） ============ */

    // ① 在线表单 —— 最推荐。任何浏览器/手机都能填，你有后台可查，还能设微信通知
    //    例：腾讯问卷、金数据、飞书表单、问卷星
    //    'https://wj.qq.com/s2/xxxxx/xxxx/'
    feedbackFormUrl: '',
    feedbackFormLabel: '在线表单',

    // ② 微信号 —— 商业价值最高。用户加你微信 = 直接建立联系 = 潜在客户
    //    例：'gds_cost'
    feedbackWechat: '',

    // ③ 微信二维码图片 —— 放在 assets/ 目录下，填相对路径
    //    例：'assets/wechat-qr.png'
    feedbackWechatQr: '',

    // ④ 邮箱 —— 会打开系统默认邮件客户端
    //    ⚠️ 注意：很多公司电脑没有配置邮件客户端，点了可能没反应，
    //    所以建议只作为补充渠道，不要当主渠道。
    //    例：'13865902776@139.com'
    feedbackEmail: '',

    /* ============ 展示信息（可选） ============ */

    // 作者署名，会显示在反馈弹窗里
    authorName: '',

    // 落地页/工具页底部想显示的联系方式（可选）
    contactLine: ''
};
